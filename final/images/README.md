# Helpful Images
Visualizations of various bits of the project. You will need a working version of $\LaTeX$ to run them unchanged. If you don't have this, go comment out the `text.useTex` bit at the beginning of each script. It'll look worse, but so be it.
 - `novelty.py`: shows how our novelty metric optimizes for maximum model confusion.
